@model BilheticaAeronauticaWeb.Models.ErrorViewModel
@{
    ViewData["Title"] = "Erro";
}

<div class="text-danger">
    <h1>@(Model.ErrorTitle ?? "Ocorreu um erro.")</h1>
    <p>@Html.Raw(Model.ErrorMessage ?? "Não foi possível processar a sua requisição.")</p>

    @if (Model.ShowRequestId)
    {
        <p><strong>ID da requisição:</strong> <code>@Model.RequestId</code></p>
    }
</div>

@if (Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT") == "Development")
{
    <div class="alert alert-warning mt-4">
        <h3>Modo de Desenvolvimento</h3>
        <p>
            O ambiente <strong>Development</strong> mostra detalhes adicionais sobre o erro.
        </p>
        <p>
            <strong>Não ative o modo de desenvolvimento em produção</strong>, pois pode revelar informações sensíveis.
            Para ativar localmente, defina a variável de ambiente <code>ASPNETCORE_ENVIRONMENT</code> como <code>Development</code> e reinicie a aplicação.
        </p>
    </div>
}